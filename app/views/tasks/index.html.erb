<h1>My tasks</h1>


<% @tasks.each do |task| %>
  <p><%= link_to task.title, task_path(task) %> <%= task.completed ? '[x]' : '[ ]' %></p>

<% end %>

<p><%= link_to 'Add new task', task_new_path %></p>

